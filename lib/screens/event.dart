import 'package:flutter/material.dart';

class EventUI extends StatelessWidget {
  const EventUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("This is EventUI"),
      ),
    );
  }
}
